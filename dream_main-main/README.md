## 如何執行本專案

1. 下載專案後，請在根目錄「自行建立」一個名為 `config.ini` 的檔案。
2. 在 `config.ini` 檔案中寫入以下內容，並填入你自己的金鑰：
3. 創虛擬環境.venv 並安裝requirements.txt
4. 使用python debugger才能正常執行
5. 如果要看.db 需安裝SQLite Viewer套件

```ini
[Gemini]
API_KEY = YOUR_GCP_API_KEY_HERE

[Pollinations]
API_KEY = YOUR_KEY